#include "MagnetMax.xcconfig"
ENABLE_BITCODE = ${MAGNETMAX_ENABLE_BITCODE}
FRAMEWORK_SEARCH_PATHS = ${MAGNETMAX_FRAMEWORK_SEARCH_PATHS}
GCC_PREPROCESSOR_DEFINITIONS = $(inherited) COCOAPODS=1
HEADER_SEARCH_PATHS = "${PODS_ROOT}/Headers/Private" "${PODS_ROOT}/Headers/Private/MagnetMax" "${PODS_ROOT}/Headers/Public" ${MAGNETMAX_HEADER_SEARCH_PATHS}
OTHER_SWIFT_FLAGS = $(inherited) "-D" "COCOAPODS"
PODS_ROOT = ${SRCROOT}
SKIP_INSTALL = YES